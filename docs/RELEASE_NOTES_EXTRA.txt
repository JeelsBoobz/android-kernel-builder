# Release footnotes, appended to BOTH the GitHub release body (as ### Notes)
# and the Telegram post (as Notes). Edit freely per release.
# Convention (keeps both surfaces native): plain text, one note per line,
# start bullets with "• ". Lines starting with # are comments (stripped).
# Telegram is HTML-escaped automatically, so write RAW text (no <b> etc.).
• SuSFS proven on android12-5.10 (boots, hides root); other trees compile green but await device testing
• BBRv3 is dormant until selected per-connection (default stays CUBIC)
• Each zip carries kernel.config (resolved) + ikconfig (extracted from the image)
• This release is built from -lts tips on all trees (no -stable content)
